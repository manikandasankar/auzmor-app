
/* - Reducer for Myaccount home page.
 * @params
 *  - state - object
 *  - action - object
 * @return
 *  - state - object
 */
export default (state = {}, action) => {
  switch (action.type) {
    default :
      return state;
  }
};
